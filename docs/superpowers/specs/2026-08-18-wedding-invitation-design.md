# 모바일 청첩장 — 설계 문서

- 작성일: 2026-08-18
- 상태: **승인됨** (2026-08-18, 브레인스토밍 세션에서 사용자 승인)
- 예식일: 2027-01-16 (토) 12:00

---

## 1. 목적

송국호 · 최은호의 결혼식을 위한 **모바일 우선 단일 페이지 청첩장**을 만들어 GitHub Pages에 배포한다.
한 번 쓰고 버리되 그 한 번을 실패하면 안 되는 물건이다. 목적함수는 **견고함과 로딩 속도**이며,
재사용성·추상화는 값어치가 없다.

## 2. 확정 사실

### 예식

| 항목 | 값 |
|---|---|
| 일시 | 2027년 1월 16일 **토요일** 낮 **12:00** |
| 장소 | 메리다웨딩컨벤션 **달리아홀 (1층)** |
| 주소 | 충청북도 청주시 청원구 내수읍 충청대로 400 |
| 대표전화 | 043-241-0003 |
| 예식 형태 | **분리예식 · 60분 간격** (식사는 별도 연회장) |
| 식사 | 뷔페 |
| 주차 | 약 1,500대 |
| 셔틀버스 | **없음** |
| 폐백 · 식전영상 | **없음** |

### 인물

| | 신랑 | 신부 |
|---|---|---|
| 본인 | 송국호 | 최은호 |
| 아버지 | 송재철 | 최재강 |
| 어머니 | 김미경 | 변종미 |
| 호칭 | **장남** | **차녀** |

### 오시는 길 (홀 공식 안내)

**자가용** — 네비게이션에 "충청북도 청주시 청원구 내수읍 충청대로 400" 입력.
중부·경부 방면: 오창IC → 청주방면 → 오동교차로 좌회전 → 구성교차로 우회전 → 200m 직진 후 좌회전.

**대중교통** — 두 경로 모두 `구성3리` 하차.

- 청주가경터미널: 도보 3분 → 시외버스정류장 승차 → **105, 105-1**
- 청주여객북부정류소: 도보 4분 → 청주대학교 정류장 승차 → **105, 105-1, 115-1, 119**

## 3. 참고 레포와 채택 범위

| 레포 | 성격 | 채택 |
|---|---|---|
| `juhonamnam/wedding-invitation` | React 19 + Vite 실전 템플릿 | **지식만 이식** — 지도앱 딥링크 URL 스킴, 달력 로직, 계좌 복사 UX, Actions Secrets 주입 패턴 |
| `revfactory/wedding-letter` | Claude Code 멀티에이전트 하네스 (vanilla 산출물) | **구조·미감·제작 방식** — 단일 페이지 vanilla, 섹션 구성, 모션 철학, QA 체크리스트 |

**채택하지 않는 것**

- `juhonamnam`의 React/Vite/SASS 빌드 체인 — 의존성 30여 개가 3개월 뒤 깨질 위험을 감수할 이유 없음
- `revfactory`의 워터컬러 PNG 자산 — 이미지 9장 16MB + BGM 4MB. 모바일 청첩장으로 낙제
- `revfactory`의 가을 팔레트(dusty rose × sage) — 1월 예식과 계절이 어긋남
- 방명록 (양쪽 다 있으나 불필요)
- BGM (불필요)

## 4. 아키텍처 결정

### AD-1. Vanilla HTML/CSS/JS. 프레임워크·npm·번들러 없음

3개월 뒤 `npm install`이 도는지 확인해야 하는 상태를 만들지 않는다.
빌드는 Python 스크립트 몇 개(폰트 서브셋·검증·용량 게이트)와 문자열 치환뿐이다.

### AD-2. 팔레트는 `theme.css` 한 파일로 격리

`style.css`에 하드코딩된 색상값은 **0개**. 모든 색은 `var(--…)`.
테마 교체 = `theme.css` 교체. 이 규칙은 CI에서 강제한다(§10).

### AD-3. 장식은 PNG가 아니라 인라인 SVG 라인아트

**무료 자산 조사 결과 (2026-08-18).** 직접 그리기 전에 오픈 라이선스 자산을 먼저 찾아봤고
전부 부적합했다. 같은 조사를 반복하지 않도록 기록해 둔다.

| 출처 | 결과 |
|---|---|
| Wikimedia Commons | PD 식물도감 다수. 표본 대지·학명 캡션·해부도가 포함된 **학술 도판**이라 잘라내야 하고, 짙은 석판화라 온실 톤과 불일치 |
| Openverse (CC0/PDM) | "watercolor flower" 계열 **0건** |
| GitHub | 웨딩 보태니컬 벡터 세트 없음 |
| Openclipart (CC0) | 결과는 있으나 **아마추어 클립아트**. 넣으면 품질이 내려감 |
| publicdomainvectors · svgrepo · rawpixel | 403 / 429 / 403 (봇 차단) |

결론: 무료·오픈 라이선스로 웨딩 수준의 수채 일러스트는 구할 수 없다. 직접 그리되
**평면적으로 보이지 않게 하는 네 가지**를 지킨다 — (1) `currentColor` 그라데이션으로 잎 안의 농담,
(2) 크기·각도 흔들림으로 개체 변이, (3) 앞뒤 3층, (4) 밑동에서 부챗살로 벌어지는 구도.
줄기를 세로로 나란히 세우면 식물도감 표본 배열처럼 보인다.

**수채 질감이 필요하면 유료 스톡을 사는 수밖에 없다.** 벡터의 상한은 '정제된 미니멀 보태니컬'이다.

겨울 식물(유칼립투스·솔가지·목화·라넌큘러스)과 약도를 SVG로 그리고 `stroke="currentColor"`를 쓴다.
결과: (a) 팔레트 교체 시 장식 색이 자동으로 따라옴, (b) 장식 하나가 2MB → 2~5KB, (c) 레티나에서 안 깨짐.

### AD-4. 외부 API 키 0개

- **지도**: 직접 그린 SVG 약도 + `네이버지도`/`카카오맵`/`T map` 앱 딥링크 + 주소 복사.
  실시간 임베드 지도는 하객이 실제로 조작하지 않으며, 딥링크가 더 나은 결과를 준다.
  딥링크는 좌표·장소ID 대신 **검색 스킴**(`nmap://search?query=`)을 쓴다 (2026-08-18 결정).
  장소ID를 박으면 API 키 없이 얻기 어렵고 홀이 등록을 바꾸면 깨진다. 앱이 없으면 스킴이
  조용히 실패하므로 1.4초 뒤에도 화면이 그대로면 웹 지도로 넘긴다.
- **공유**: `og:` 메타태그(카톡에 링크를 붙이면 미리보기 카드가 자동 생성됨) + 링크 복사 버튼.
  카카오 JS SDK는 "페이지 안의 공유 버튼"만 추가할 뿐이며, 실제 전파는 신랑신부가 직접 뿌리는 링크가 대부분이다.

### AD-5. RSVP 백엔드는 Google Apps Script → Google Sheets

집계 화면을 만들지 않아도 되는 것이 결정적 이유. 제출이 시트에 쌓이고 `SUM` 한 줄로 정산이 끝난다.
Supabase 등 무료 티어 DB는 **유휴 시 프로젝트 일시정지** 정책이 있어 장기 방치 후 필요한 순간에 죽을 위험이 있다.
(구현 시 현행 정책 재확인할 것.)

## 5. 섹션 구성

모바일 세로 스크롤 단일 페이지. 데스크톱은 `max-width: 480px` 중앙 카드.

| # | 섹션 | 내용 |
|---|---|---|
| 1 | **히어로** (100vh) | 겨울식물 SVG 프레임 · `송국호 & 최은호` · `2027.01.16 SAT 12:00` · 메리다웨딩컨벤션 달리아홀 · 옅은 눈 입자. **10월에 웨딩사진으로 교체 예정** |
| 2 | **모시는 글** | 인사말 4~6문장 |
| 3 | **신랑 신부** | 송재철 · 김미경의 **장남** 국호 / 최재강 · 변종미의 **차녀** 은호 |
| 4 | **예식일** | 2027년 1월 달력에 16일 표시 + D-day 카운트다운 |
| 5 | **갤러리** | 가로 스와이프 + 탭하여 확대(핀치 줌). 현재 플레이스홀더 5칸 |
| 6 | **오시는 길** | SVG 약도 · 주소 복사 · 지도앱 3종 딥링크 · 자가용/대중교통 · 주차 1,500대 · 달리아홀 1층 · **결빙/눈 안내** · **여유있게 도착 안내** |
| 7 | **마음 전하실 곳** | 신랑측/신부측 접이식, 계좌 4개, 복사 버튼 |
| 8 | **참석 의사** | §6 폼 |
| 9 | **공유** | 링크 복사 버튼 |
| 10 | **푸터** | 감사 인사 한 줄 |

### 인사말 초안 (v0 — 구현 단계에서 확정)

```
겨울의 한가운데,
서로에게 가장 따뜻한 사람이 되기로 했습니다.

오래 알아온 두 사람이
이제 같은 방향을 보며 걷습니다.

새해의 첫 달, 저희가 시작하는 자리에
귀한 걸음 해 주신다면
따뜻한 밥 한 끼 나누며 인사드리겠습니다.
```

톤: 차분한 경어체. 옛스러운 정중체("일가 친지를 모시고…")도, 캐주얼한 반말체도 피한다.
낮 12시 뷔페라 마지막 줄이 자연스럽게 붙는다.

### 계좌 표기 형식

```
신랑     송국호  / 국민은행    __ACCT_GROOM__
신랑 혼주 송재철  / 우리은행    __ACCT_GROOM_HOST__
신부     최은호  / 카카오뱅크  __ACCT_BRIDE__
신부 혼주 최재강  / 농협        __ACCT_BRIDE_HOST__
```

라벨은 "아버지"가 아니라 **"혼주"**를 쓴다. 계좌는 4개(양가 2개씩).

## 6. RSVP 데이터 모델

### 폼

```
구분        ○ 신랑측  ○ 신부측              (필수)
성함        ____________                     (필수)
참석 여부   ○ 참석합니다  ○ 어렵습니다      (필수)

  ↓ '참석' 선택 시에만 펼침
식사하실 분        − 2 +
답례품 받으실 분   − 1 +
  → "총 3분 참석" 자동 표시

전하고 싶은 말  ____________               (선택, 한 줄)
```

**총 참석 인원을 따로 묻지 않고 두 숫자의 합으로 계산한다.** 이유:
(a) 하객이 헷갈릴 여지가 없고, (b) 합이 안 맞는 입력이 구조적으로 불가능하며,
(c) 웨딩홀에 넘길 숫자(식사 n명 / 답례품 n개)가 가공 없이 그대로 나온다.

**연락처는 받지 않는다.** 하객 전화번호를 모으는 순간 우리가 관리 책임을 지는 개인정보가 된다.

### 시트 스키마

| 열 | 타입 |
|---|---|
| timestamp | ISO8601 |
| side | `groom` \| `bride` |
| name | string |
| attending | `yes` \| `no` |
| meal_count | int (불참 시 0) |
| gift_count | int (불참 시 0) |
| message | string (선택) |

### 실패 처리

제출 실패 시 "카카오톡으로 알려주세요" 폴백 문구를 띄운다. 막다른 길을 만들지 않는다.
Apps Script는 `Content-Type: text/plain`으로 POST해 CORS preflight를 회피한다.

## 7. 디자인 시스템

### 기본 테마: **온실 (Winter Garden)**

```css
:root {
  --color-bg:        #F5F4EF;  /* 페이지 베이스 */
  --color-surface:   #E5E6DC;  /* 카드/섹션 분리 */
  --color-secondary: #CFC3B4;  /* 마른 목화빛 보조 */
  --color-primary:   #7F9078;  /* winter sage — 강조 */
  --color-accent:    #3F4E3C;  /* deep pine — 액션 */
  --color-text:      #2C322C;
  --color-muted:     #7E8579;
}
```

### 대체 테마: **설야 (Snow & Ink)** — `theme.css` 교체만으로 전환

```css
:root {
  --color-bg:        #F6F8F8;
  --color-surface:   #E9EEEF;
  --color-secondary: #AEBCC0;
  --color-primary:   #2F4A43;  /* deep pine */
  --color-accent:    #16232B;  /* ink */
  --color-text:      #232B2E;
  --color-muted:     #7B888C;
}
```

**사진이 나온 뒤(10월) 최종 테마를 결정한다.** 그래서 §AD-2·AD-3의 격리가 필수다.

### 모션

"조용히 피어나는" — 빠르고 화려한 모션 금지. 전환은 ease-out 600~900ms.

- 배경: **아주 옅은 눈 입자**가 아주 느리게 (밀도 낮게, opacity 0.15~0.35). 두 테마 공통
- 스크롤: IntersectionObserver 진입 시 fade-in + translateY(20px → 0)
- 마이크로: 계좌 복사 시 체크 마크 + 햅틱(가능 시)
- `prefers-reduced-motion: reduce` → 눈 입자 OFF, fade-in 즉시 표시

## 8. 타이포그래피 · 폰트 서브셋

### 폰트 계통을 둘로 분리 — 두부(□) 방지의 핵심

```css
--font-display: "NanumMyeongjo-subset", "Apple SD Gothic Neo", "Malgun Gothic", serif;
--font-ui:      -apple-system, BlinkMacSystemFont, "Apple SD Gothic Neo",
                "Malgun Gothic", "Noto Sans KR", sans-serif;
```

- `--font-display` = **고정 텍스트 전용** (인사말, 이름, 날짜, 섹션 제목)
- `--font-ui` = **사용자 문자열이 지나갈 수 있는 모든 곳**

영문 세리프(Cormorant Garamond)를 서브셋해 self-host한다. 둘 다 OFL이라 서브셋 재배포 가능.
외부 요청 0건이 되어 CDN 정책 변화에도 안 깨진다.

**스크립트체(Allura)는 쓰지 않는다** (2026-08-18 구현 중 결정). 청첩장에서 필기체 영문은
가장 흔한 장식이고, 빼는 편이 온실 컨셉의 절제와 맞는다. 약 10KB도 아낀다.
숫자는 `lnum`(라이닝)을 강제한다 — Cormorant 기본값인 올드스타일 숫자는 `01`이 `OI`로
읽혀서 어르신 하객에게 불리하다.

### 두부 방지 4층

1. **스코프 분리** — 사용자 문자열이 들어가는 DOM에 `data-user-content` 속성을 달고
   `[data-user-content] { font-family: var(--font-ui) !important }`.
   **특히 "○○○님, 참석으로 접수되었습니다" 같은 제출 확인 메시지가 최대 위험 지점이다.**
   입력창만 막고 여기를 놓치기 쉽다. 토스트·에러 문구도 포함.
2. **서브셋을 빌드 산출물로** — `tools/subset-fonts.py`가 `index.html`에서 실제 쓰인 문자를 추출해
   `pyftsubset`에 먹인다. 카피를 고치면 서브셋이 자동으로 따라온다. 수동 동기화를 없앤다.
3. **CI 게이트** — `tools/check-glyphs.py`가 고정 텍스트의 모든 코드포인트를 서브셋 폰트와 대조.
   하나라도 빠지면 **빌드 실패**.
4. **폴백 스택** — 위 셋이 다 뚫려도 마지막 방어선. iOS/Android/Windows/macOS 모두 한글 시스템 폰트를
   갖고 있어 두부는 구조적으로 발생 불가. 최악이 "글자 하나가 다른 폰트로 보임"이다.

추가로 서브셋 생성 시 **한국인 성명 빈출 음절을 여유분으로 얹는다.** 몇 KB로 안전 마진을 산다.

## 9. 자산 전략

- **SVG 라인아트** — 겨울식물 장식, 섹션 디바이더, 약도. 인라인 + `currentColor`
- **사진** — AVIF 우선, WebP 폴백, `srcset` 반응형, `loading="lazy"`. 히어로만 `preload`
- **플레이스홀더** — 10월에 전부 교체되므로 **싸게 만들고 교체를 쉽게** 한다.
  파일명·비율(4:5)을 고정해두고 파일만 갈아끼우면 되게 할 것
- **OG 이미지** — 카톡 미리보기용 1장. 1200×630

## 10. 용량 예산 — CI에서 강제

**코드는 gzip 전송 크기로 잰다** (2026-08-18 구현 중 결정). GitHub Pages가 텍스트 자산을
항상 압축해 보내므로 하객의 로딩 속도를 정하는 값이 그쪽이다. 비압축 크기로 재면
주석을 지우는 것이 성능 개선으로 잘못 계산된다. woff2·PNG는 이미 압축돼 있어 원본 크기로 잰다.

| 항목 | 예산 | 측정 |
|---|---|---|
| HTML + CSS + JS | 30 KB | gzip |
| 폰트 (서브셋 2종) | 60 KB | 원본 |
| SVG 장식 · 약도 | (HTML에 인라인이라 위에 포함) | — |
| 히어로 이미지 | 120 KB | 원본 |
| **첫 화면 소계** | **~270 KB** | |
| 갤러리 5장 (lazy, AVIF) | 각 ~100 KB | 원본 |
| **전체** | **~800 KB** | |

`revfactory`가 약 20MB인 것 대비 **25배 축소**가 목표.
`tools/check-budget.sh`가 초과 시 배포를 실패시킨다. 숫자로 관리하지 않으면 반드시 샌다.

**CI 게이트 4종 (모두 배포 차단)**
1. 용량 예산 초과 — `tools/check-budget.sh`
2. 폰트 글리프 커버리지 미달 — `tools/check-glyphs.py`
3. `style.css`에 하드코딩된 색상 리터럴 존재 — `tools/check-colors.sh`
4. **저장소에 실제 계좌번호 존재** — `tools/check-privacy.sh` (2026-08-18 추가).
   규칙 1은 사람의 주의력에 맡기기엔 대가가 크다. 자리표시자 4개가 그대로인지,
   계좌 섹션에 더미가 아닌 숫자가 없는지 기계가 확인한다.

## 11. 개인정보 취급

### 원칙

**계좌번호·전화번호는 git에 절대 들어가지 않는다.**

- `index.html`에는 `__ACCT_GROOM__` 같은 자리표시자만 둔다
- 실제 값은 GitHub Actions Secrets → 배포 시 치환
- 로컬 개발용으로 전부 `000000-00-000000`인 더미 값 파일을 커밋
- 결과: 개발 중 실제 계좌가 저장소·히스토리·에이전트 컨텍스트 어디에도 없다
- fork가 떠도 개인정보는 딸려가지 않는다

### 노출 축소

- `robots.txt` + `<meta name="robots" content="noindex,nofollow">` — 검색 색인 제외
- **URL은 `/wedding`이 아니라 추측하기 어려운 슬러그** (레포 이름 = URL 슬러그)
- 계좌는 클릭해서 펼치기 (기본 접힘)

**알려진 한계 (수용함):** GitHub Pages 프로젝트 사이트는 무료 계정에서 레포가 public이어야 하고,
public 레포는 `github.com/GH-Song?tab=repositories`에 노출된다. 슬러그는 URL 추측과 검색 색인을 막을 뿐
프로필 열람은 막지 못한다. 레포에 개인정보가 없으므로 실질 위험은 낮다고 판단해 수용한다.

### 종료 절차 (식 후)

1. 개인정보 섹션 제거 후 재배포
2. 레포를 로컬로 백업
3. **GitHub에서 레포 삭제** — 히스토리까지 함께 사라진다

## 12. 배포

- **GitHub Pages**, 레포 `GH-Song/<슬러그>`, URL `https://gh-song.github.io/<슬러그>/`
- **로컬 경로: `/Users/gookho/Projects/wedding/`** (원격 슬러그와 무관)
- GitHub Actions: 폰트 서브셋 생성 → 이미지 최적화 → Secrets 치환 → CI 게이트 3종 → Pages 배포
- `.nojekyll` 유지
- Secrets: `ACCT_GROOM`, `ACCT_GROOM_HOST`, `ACCT_BRIDE`, `ACCT_BRIDE_HOST` (+ 연락처 확정 시 추가)

## 13. 접근성 · 호환성

**필수 검증 대상 3종**

1. iOS Safari
2. Android Chrome
3. **카카오톡 인앱 브라우저** ← 최우선.
   청첩장은 대부분 카톡 링크로 열린다. 여기서 깨지면 하객 절대다수가 깨진 것을 본다.

**기준**

- `prefers-reduced-motion: reduce` 분기
- WCAG 2.1 AA 명도 대비
- 본문 16px 이상 (어르신 하객 배려)
- 375px ~ 1440px 반응형
- 터치 타깃 44×44px 이상

## 14. QA 체크리스트

- [ ] 이름·부모 함자 오타 없음 (송국호/최은호, 송재철·김미경, 최재강·변종미, 장남/차녀)
- [ ] 예식일·시각·장소·주소 일치 (2027-01-16 토 12:00 / 달리아홀 1층)
- [ ] 계좌 4개 자리표시자가 실제 값으로 치환됨
- [ ] 지도앱 딥링크 3종이 실기기에서 앱을 연다
- [ ] 계좌 복사 · 주소 복사 · 링크 복사 동작
- [ ] RSVP 제출 → 시트에 정확히 기록. 불참 시 인원 0
- [ ] RSVP 확인 메시지에 한글 이름이 두부 없이 표시됨
- [ ] 갤러리 스와이프 · 라이트박스 핀치 줌
- [ ] `prefers-reduced-motion`에서 눈 입자 정지
- [ ] 카톡 인앱 브라우저 전 섹션 정상
- [ ] `og:` 미리보기 카드가 카톡에서 정상 렌더
- [ ] 용량 예산 준수 (실측)
- [ ] Lighthouse 모바일 성능 측정

## 15. 일정

| 단계 | 시점 |
|---|---|
| 뼈대 + 온실 테마 + 더미 콘텐츠 | 2026-08~09 |
| *폰트 파이프라인을 히어로 단계로 앞당김* — 서체가 정해지지 않으면 타이포 스케일을 확정할 수 없다 | 2026-08-18 |
| 약도 SVG · 겨울식물 라인아트 | |
| RSVP 백엔드 · 폰트 서브셋 · CI 게이트 | |
| 슬러그 확정 후 첫 배포 | |
| *— 웨딩 촬영 —* | 2026-09 |
| 사진 교체 · **테마 최종 결정** | 2026-10 중순 |
| **계좌 Secrets 주입 · 실배포** | **2026-11 말** |
| *— 예식 —* | 2027-01-16 |
| 개인정보 제거 → 백업 → 레포 삭제 | 식 후 |

**실질 마감은 예식일이 아니라 2026년 11월 말이다.** 청첩장은 예식 6~8주 전에 뿌린다.

## 16. 미확정 항목

구현을 막지 않는다. 비워두고 진행하다 채운다.

| 항목 | 필요 시점 |
|---|---|
| **혼주 계좌 명의** — 현재 아버지(송재철·최재강)로 가정함. 어머니 명의면 수정 필요 | 배포 전 |
| **연회장 층수** (분리예식이라 안내 필요) | 홀에 확인 |
| 주차 무료 여부 | 홀에 확인 |
| 신랑신부 연락처 노출 여부 (`tel:` 링크) | 배포 전 |
| URL 슬러그 확정 | 첫 배포 전 |
| 실제 웨딩 사진 | 2026-10 |
| 최종 테마 (온실 / 설야) | 사진 수령 후 |
| RSVP 마감일 표기 여부 | 배포 전 |

## 17. 이 문서의 권위

구현 중 이 문서와 충돌하는 판단이 필요하면 **문서를 먼저 고치고** 진행한다.
특히 §10 용량 예산, §11 개인정보 원칙, §8 폰트 스코프 분리는 **협상 대상이 아니다.**
