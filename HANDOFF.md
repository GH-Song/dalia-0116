# 핸드오프 — 다음 세션에서 읽을 것

갱신: 2026-08-18 (v2 디자인 개편 세션)

## 현재 상태

**전 섹션 동작, 실배포 완료.** https://gh-song.github.io/dalia-0116/

- 레포: `GH-Song/dalia-0116` (public — 무료 Pages 조건). 로컬은 `/Users/gookho/Projects/wedding/`
- `docs/superpowers/specs/2026-08-18-wedding-invitation-design.md` — 승인된 설계 문서(**v2 개정 포함**). **모든 결정의 근거**
- `CLAUDE.md` — 협상 불가 규칙 4가지

10개 섹션(히어로·모시는 글·신랑신부·예식일·갤러리·오시는 길·마음 전하실 곳·참석 여부·공유·푸터)이
전부 들어가 있고 CI 게이트 4종을 통과합니다.

## v2 디자인 개편 (2026-08-18, 사용자 피드백)

녹색 온실 → **동백(따뜻한 겨울) 웜톤**으로 전면 교체. revfactory 타이포 문법 채택.

- **팔레트**: 샴페인 아이보리 × 블러시 × 더스티 코랄 × 로즈우드 (`theme.css` 교체. 대비 실측표 주석 참조)
- **타이포**: 서브셋 4종 — 명조 400(본문) · **명조 700**(제목·이름) · Cormorant(아이브로우·숫자) · **Allura**(스크립트 악센트 3곳)
  - `data-face="bold|script"` 속성이 **스타일 훅이자 서브셋 추출 마커** (설계 문서 §8 v2). 마커 없이 볼드/스크립트를 쓰면 글리프 게이트가 잡습니다
- **히어로**: 동백 garland → We're getting married → SAVE THE DATE → **송국호 & 최은호** (`와` 대신 스크립트 `&`) → 순차 등장 애니메이션
- **장식**: `tools/make-florals.py` (make-botanicals.py 대체). 반투명 꽃잎 겹쌓기 방식. 부위별 `.flora-*` 클래스가 테마 색 상속.
  **인라인 SVG를 다시 만들 때**: 생성기 수정 → 출력을 index.html 의 해당 `<svg class="flora flora-…">` 와 통째로 교체
- **섹션 템플릿**: 영문 아이브로우 + 한글 볼드 제목, 코너 플로럴(교대), 라운드 카드·필 버튼, data-rv 스태거 진입
- **문구**: 모시는 글 재작성("볕이 드는 자리"), 커플 카드 한 줄 소개, D-day `D - n` 형식
- **예산 조정** (설계 문서 §10 v2): 코드 30→35KB gzip, 폰트 60→85KB. 첫 화면 270/전체 800 상한 불변.
  **현재 코드 34.7/35KB — 여유 0.3KB뿐.** HTML 을 키우는 편집 후엔 반드시 실측하세요

## 규칙 4가지 현황

| 규칙 | 상태 | 강제 수단 |
|---|---|---|
| 1. 계좌 git 금지 | 자리표시자만. 배포본엔 더미 주입됨 | `tools/check-privacy.sh` |
| 2. 색상 리터럴 0개 | 통과 | `tools/check-colors.sh` |
| 3. 사용자 문자열 폰트 | 통과. 희귀 음절로 실사격 검증 | `tools/check-glyphs.py` |
| 4. 용량 | 첫 화면 65KB/270KB, 전체 97KB/800KB | `tools/check-budget.sh` |

`tools/` 스크립트는 로컬에서 그대로 돌릴 수 있고 Actions 가 같은 것을 씁니다.

## 로컬에서 보기

```bash
python3 tools/subset-fonts.py && python3 tools/make-og.py && python3 -m http.server 8137
```

폰트는 빌드 산출물이라 `assets/fonts/` 가 gitignore 되어 있습니다. 원본은 스크립트가 자동으로 받습니다.

## 남은 일

### 사용자 확인이 필요한 것 (배포 전)

- **연회장 층수** — 분리예식이라 "예식 후 ○층 연회장에서 식사" 안내가 필요합니다. 홀에 확인
- **주차 무료 여부** — 현재 "약 1,500대 주차 가능"만 적혀 있습니다
- **혼주 계좌 명의** — 지금은 아버지(송재철·최재강)로 가정. 어머니 명의면 수정
- **신랑신부 연락처 노출 여부** — 현재 웨딩홀 대표번호만 `tel:` 링크
- **RSVP 마감일 표기 여부**

### 완료됨

- **RSVP 백엔드** — Google Apps Script + 시트 연결 완료(2026-08-18). 실기기·브라우저에서
  제출 → 시트 기록까지 검증했습니다. 엔드포인트는 Actions Secret `RSVP_ENDPOINT`.
  스크립트 원본은 `tools/rsvp-apps-script.gs` (저장소 버전은 SHEET_ID 를 비워둡니다 — 공개 레포).
  코드를 고치면 Apps Script 에서 **배포 관리**로 기존 배포를 수정해야 URL 이 유지됩니다.

### 기능 미완

1. **계좌 Secrets** — 레포 Settings > Secrets 에 `ACCT_GROOM` / `ACCT_GROOM_HOST` /
   `ACCT_BRIDE` / `ACCT_BRIDE_HOST` 를 넣으면 다음 배포부터 실제 값이 들어갑니다.
   넣기 전까지는 더미가 나옵니다. **채팅에 계좌번호를 붙여넣지 마세요**
2. **사진** — 갤러리 5칸과 히어로가 자리표시자입니다. 2026-10 촬영본으로 교체.
   `assets/img/gallery-{1..5}.svg` 를 같은 4:5 비율 이미지로 갈아끼우면 됩니다
3. **테마 최종 결정** — 사진 수령 후 확정. 기본은 동백(웜톤). 사진 톤이 차가우면 설계 문서 §7의
   이전 팔레트(온실/설야)로 `theme.css` 만 교체해 되돌릴 수 있습니다.
   대비 실측표가 파일 상단 주석에 있으니 교체 시 다시 재세요. `tools/make-og.py` 색상도 함께

### QA 미실시

**실기기 검증을 아직 안 했습니다.** 설계 문서 §14 체크리스트 기준으로:

- iOS Safari / Android Chrome / **카카오톡 인앱 브라우저**
- 지도앱 딥링크 3종이 실기기에서 실제로 앱을 여는지 (검색 스킴이라 앱 미설치 시 폴백도)
- 갤러리 핀치 줌 · 라이트박스 스와이프
- 카톡에 링크를 붙였을 때 og 카드가 뜨는지
- `prefers-reduced-motion` 에서 눈 입자 정지

## 알아둘 것

- **달력은 정적 HTML 입니다.** JS 가 죽어도 하객이 날짜를 봐야 하기 때문입니다. D-day 만 JS
- **지도 딥링크는 검색 스킴**(`nmap://search?query=`). 좌표·장소ID 를 안 씁니다 (API 키 0개 원칙).
  더 정확하게 하려면 나중에 장소ID 를 넣을 수 있습니다
- **Apps Script 는 POST 를 처리한 뒤 googleusercontent.com 으로 302 리다이렉트합니다.**
  curl 로는 그 페이지를 못 읽어 실패처럼 보이지만 쓰기는 이미 끝난 상태입니다.
  브라우저는 정상 처리하므로, 검증은 반드시 브라우저나 행 수(`/exec` GET)로 하세요
- **식사 인원 + 답례품 인원 = 총 참석 인원.** 식사는 안 하고 답례품만 받고 가시는 하객이 있어서입니다
- **Cormorant 는 라이닝 숫자를 강제**해야 합니다. 기본 올드스타일이라 `01` 이 `OI` 로 읽힙니다
- **나눔명조에 U+2212(−) 가 없습니다.** 폰트 의존 기호 대신 SVG 아이콘을 쓰세요. 글리프 게이트가 잡아줍니다

## 식 후 종료 절차

1. 개인정보 섹션 제거 후 재배포
2. 레포 로컬 백업
3. GitHub 에서 레포 삭제 (히스토리까지 사라집니다)
