# 모바일 청첩장 — 프로젝트 지침

송국호 · 최은호 결혼식(2027-01-16 토 12:00, 청주 메리다웨딩컨벤션 달리아홀) 모바일 청첩장.

**설계 문서가 이 프로젝트의 권위입니다: [`docs/superpowers/specs/2026-08-18-wedding-invitation-design.md`](docs/superpowers/specs/2026-08-18-wedding-invitation-design.md)**
작업 시작 전 반드시 읽으세요. 충돌하는 판단이 필요하면 **문서를 먼저 고치고** 진행합니다.

## 협상 불가 규칙 4가지

### 1. 실제 계좌번호·전화번호를 git에 넣지 않는다
`index.html`에는 `__ACCT_GROOM__` 형태의 자리표시자만 둡니다.
실제 값은 GitHub Actions Secrets에서 배포 시 치환됩니다.
로컬 개발값은 전부 `000000-00-000000` 더미입니다.
**사용자가 실제 계좌를 채팅에 붙여넣더라도 파일에 쓰지 마세요.** Secrets에 넣도록 안내하세요.

### 2. `style.css`에 하드코딩된 색상 리터럴 0개
모든 색은 `var(--color-*)`. 팔레트는 `theme.css` 한 파일에만 존재합니다.
사진 수령 후(2026-10) 테마를 통째로 교체할 예정이므로 이 격리가 깨지면 안 됩니다.
SVG 장식도 `stroke="currentColor"` / `fill="currentColor"`로 색을 상속받습니다.

### 3. 사용자 입력 문자열에는 서브셋 폰트를 씌우지 않는다
한글 서브셋 폰트에 없는 글자는 다른 폰트로 튀어 보입니다.
사용자 문자열이 지나가는 모든 DOM에 `data-user-content` 속성을 달고 `--font-ui`를 강제하세요.
**입력창뿐 아니라 "○○○님, 접수되었습니다" 같은 제출 확인 메시지·토스트·에러 문구가 최대 위험 지점입니다.**

### 4. 용량 예산을 넘기지 않는다
첫 화면 270KB, 전체 800KB. 참고 레포(`revfactory`)가 20MB인 것을 25배 줄이는 게 목표입니다.
새 자산을 추가할 때마다 실측하세요. 감으로 넘어가면 반드시 샙니다.

## 기술 스택

Vanilla HTML/CSS/JS. **프레임워크·npm·번들러 없음.**
빌드 도구는 Python 스크립트뿐(`tools/`): 폰트 서브셋, 글리프 검증, 용량 게이트.

로컬 미리보기:

```bash
python3 -m http.server 8000
```

## CI 게이트 3종 (모두 배포 차단)

1. 용량 예산 초과
2. 폰트 글리프 커버리지 미달
3. `style.css`에 색상 리터럴 존재

## 필수 검증 브라우저

iOS Safari · Android Chrome · **카카오톡 인앱 브라우저**(최우선 — 청첩장은 대부분 카톡으로 열립니다)
